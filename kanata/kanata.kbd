;; Version 1.11.0 does not have tap-hold-require-prior-idle yet. Requires building from main branch.
(defcfg
    process-unmapped-keys yes
    tap-hold-require-prior-idle 150
)

(defsrc
    caps a s d f g h j k l ;
)

(defvar
    tap-hold 100
    hold-timeout 150
)

(deftemplate
    hrmod (key hold-action prior-idle) (
        tap-hold-release $tap-hold $hold-timeout $key $hold-action (
            require-prior-idle $prior-idle
        )
    )
)
(deftemplate
    hrmod-repeat (key hold-action prior-idle) (
        tap-dance 100 ((t! hrmod $key $hold-action $prior-idle) (multi $key))
    )
)

(defalias
    caps (t! hrmod esc (layer-toggle movement) 150)
    a (t! hrmod a lmet 150)
    s (t! hrmod s lalt 150)
    d (t! hrmod d lsft 50)
    f (t! hrmod f lctl 150)

    j (t! hrmod j rctl 150)
    k (t! hrmod k rsft 50)
    l (t! hrmod l lalt 150)
    ; (t! hrmod ; rmet 150)
)

(deflayer default
    @caps @a @s @d @f _ _ @j @k @l @;
)

(deflayermap movement
    u PageUp
    d PageDown
    h left
    j down
    k up
    l right
)

(defhands
  (left  q w e r t a s d f g z x c v b)
  (right y u i o p h j k l ; n m , . /)
)

;; tap-hold-opposite-hand-release has a bug where the tap key is sent if I don't press another key before the hold timeout expires.
;; (defalias
;;     a (tap-hold-opposite-hand-release 150 a lmet)
;;     s (tap-hold-opposite-hand-release 150 s lalt)
;;     d (tap-hold-opposite-hand-release 150 d lsft)
;;     f (tap-hold-opposite-hand-release 150 f lctl)

;;     j (tap-hold-opposite-hand-release 150 j rctl)
;;     k (tap-hold-opposite-hand-release 150 k rsft)
;;     l (tap-hold-opposite-hand-release 150 l lalt)
;;     ; (tap-hold-opposite-hand-release 150 ; rmet)
;; )

